<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gameloft9.demo.dataaccess.dao.system.DepotInventoryMapper">

    <resultMap id="BaseResultMap" type="com.gameloft9.demo.dataaccess.model.system.DepotInventoryTest">
        <id column="ID" property="id" jdbcType="VARCHAR"/>
        <result column="GOODS_TYPE" property="goodsType" jdbcType="INTEGER"/>
        <result column="GOODS_ID" property="goodsId" jdbcType="VARCHAR"/>
        <result column="GOODS_NUMBER" property="goodsNumber" jdbcType="VARCHAR"/>
    </resultMap>

    <!--将数据库里所有字段打包好，嵌入下列的sql语句中-->
    <sql id="Base_Column_List">
        ID,GOODS_TYPE,GOODS_ID,GOODS_NUMBER
    </sql>
    <!--获取id-->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_depot_inventory t
        WHERE t.ID=#{id}
    </select>
    <!--添加-->
    <insert id="insert" parameterType="com.gameloft9.demo.dataaccess.model.system.DepotInventoryTest">
        INSERT INTO t_depot_inventory(ID,GOODS_TYPE,GOODS_ID,GOODS_NUMBER)
        VALUES (#{id},#{goodsType},#{goodsId},#{goodsNumber})
    </insert>
    <!--删除-->
    <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
        DELETE FROM t_depot_inventory
        WHERE ID = #{id}
    </delete>
    <!--修改-->
    <update id="update" parameterType="com.gameloft9.demo.dataaccess.model.system.DepotInventoryTest">
        UPDATE t_depot_inventory
        SET GOODS_TYPE=#{goodsType},GOODS_ID=#{goodsId},GOODS_NUMBER=#{goodsNumber}
        WHERE ID = #{id}
    </update>
    <!--总条数查找所有-->
    <select id="countGetAll" resultType="int" parameterType="map">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 04 11:17:05 CST 2017.
        -->
        select
        count(1)
        from t_depot_inventory t
        <where>
            <if test="goodsType != null and goodsType != ''">
                and t.GOODS_TYPE = #{goodsType}
            </if>
        </where>
    </select>
    <!--分页的查找所有-->
    <select id="selectAll" resultMap="BaseResultMap">
        select * from t_depot_inventory t
        <where>
            <if test="goodsType != null and goodsType != ''">
                and t.GOODS_TYPE = #{goodsType}
            </if>
        </where>
        order by t.id asc
        limit #{start,jdbcType=DECIMAL},#{end,jdbcType=DECIMAL}
    </select>
</mapper>
